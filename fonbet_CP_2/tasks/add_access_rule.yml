# Add access rule to CP
- name: login
  uri:
    url: "https://192.168.1.100/web_api/v1.6/login"
    validate_certs: false
    method: POST
    body_format: json
    body:
      user: "{{ cp_user }}"
      password: "{{ cp_password }}"
  register: loginoutput

- set_fact:
    sid: "{{ loginoutput.json.sid }}"

- name: add_access_rule
  uri:
    url: "https://192.168.1.100/web_api/v1.6/add-access-rule"
    validate_certs: false
    method: POST
    body_format: json
    headers:
      X-chkp-sid: "{{ sid }}"
    body:
      name: "{{ cp_rule.value.name }}"
      layer: Network
      position:
        bottom: "From Ansible"
      track:
        type: Log
      time: "{{ cp_rule.value.time_name | default(omit) }}"
      source:
        - "{{ cp_rule.value.source_name }}"
      destination:
        - "{{ cp_rule.value.destination_name }}"
      service:
        - "{{ cp_rule.value.service_name_1 | default(omit) }}"
        - "{{ cp_rule.value.service_name_2 | default([]) }}"
        - "{{ cp_rule.value.service_name_3 | default([]) }}"
        - "{{ cp_rule.value.service_name_4 | default([]) }}"
      action: "{{ cp_rule.value.action }}"
      comments: "Geneted by Ansible from the board {{ jira_board }} and case ID {{ jira_case }}"
  with_dict: "{{ checkpoint_parsed_csv.dict }}"
  loop_control:
    loop_var: cp_rule


- name: publish
  uri:
    url: "https://192.168.1.100/web_api/v1.6/publish"
    validate_certs: false
    method: POST
    body_format: json
    headers:
      X-chkp-sid: "{{ sid }}"
    body:
      {}
